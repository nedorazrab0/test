name: mkng

# on: workflow_dispatch
on: push

permissions:
  contents: write

jobs:
  cmprsng:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: gh --version
      - name: mkng n fle
        run: |
          tar -cvf ./.rch-nstll.tar ./*
          zstd --ultra -vf20 ./.rch-nstll.tar
      - name: rlsng
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload v1.2.3 './.rch-nstll.tar.zst#msds' --clobber --notes 'hui'
#        run: |
#          git config user.name 'github-actions[bot]'
#          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
#          git add .rch-nstll.tar.zst
#          git commit -m 'hui'
#          git push

