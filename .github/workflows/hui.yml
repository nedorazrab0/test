name: RLS
on: push
permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
     - name: dc
       run: |
         curl -LfZ -o hrui.zip 'https://github.com/kkrypt0nn/wordlists/raw/refs/heads/main/wordlists/famous/rockyou.zip'
         unzip hrui.zip

         sudo apt update
         sudo apt install pigz
         time zip -9rv rockyou.txt
         ls -lh hrui.zip
         time pigz -11vf --zip rockyou.txt
         ls -lh hrui.zip